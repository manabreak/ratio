package me.manabreak.ratio.editor;

/**
 * Denotes the panel in the editor
 */
public enum EditorPanel {
    LEFT, // Left portion of the main content
    RIGHT, // Right portion of the main content
    TOP, // Toolbar above the main content
    NONE, // Not a panel, but a floating window
    BOTTOM // Bottom bar below the main content
}
